{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Lldb_debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/rust-tutorial.exe",
            "args": [],
            "cwd": "${workspaceRoot}"
        }
    ]
}